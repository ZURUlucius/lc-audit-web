services:
  - type: web
    name: lc-audit-web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: CLOUD
        value: "1"
      - key: PORT
        value: 10000
    healthCheckPath: /health
    plan: free
